{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <main class="relative h-screen w-screen flex flex-col justify-center items-center overflow-auto">
        <section class="relative w-full h-full flex justify-center z-10">
            <div class="relative flex flex-col w-full">
                {% include 'admin/header.html.twig' with {'title': 'Edit User', 'button_link': path('app_user_index')} %}
                <div class="px-8 py-4 w-full flex justify-center">
                    {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
        </section>
    </main>
{% endblock %}
